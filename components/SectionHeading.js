

function SectionHeading(props) {
    return(
        <div>
            <h1 className = "section-heading">
                {props.text}
            </h1>
        </div>
    );
}

export default SectionHeading;
